import { StepProps } from "..//rent.types";

const Step3 = ({ formData, setFormData, error, setError }: StepProps): JSX.Element => {
    return (
        <>
        </>
    );
}

export default Step3;