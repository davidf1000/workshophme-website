import { StepProps } from "..//rent.types";

const Step4 = ({ formData, setFormData, error, setError }: StepProps): JSX.Element => {
    return (
        <>
        </>
    );
}

export default Step4;